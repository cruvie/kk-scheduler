edition = "2023";

package kk_schedule;

option go_package = "/kk_schedule";

service KKScheduleTrigger {
  rpc Trigger(Trigger.Input) returns (Trigger.Output);
}

message Trigger {
  message Input {
    string funcName = 1;
  }
  message Output {
  }
}