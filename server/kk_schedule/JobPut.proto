edition = "2023";
package kk_schedule;

option go_package = "/kk_schedule";

import "kk_schedule/Job.proto";
import "kk_schedule/Base.proto";

message JobPut {
  message Input {
    PBRegisterJob Job = 1;
  }

  message Output {
  }
}
