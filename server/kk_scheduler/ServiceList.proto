edition = "2023";

package kk_scheduler;

option go_package = "/kk_scheduler";

import "kk_scheduler/Base.proto";

message ServiceList {
  message Input {
  }

  message Output {
    repeated PBRegisterService ServiceList = 1;
  }
}