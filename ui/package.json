{
  "name": "kk-schedule",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "npm install": " npm install --force",
    "install check-updates": "npm install -g npm-check-updates",
    "check-updates": "npm-check-updates",
    "update package.json deps": "ncu -u",
    "cache clean": " npm cache clear --force"
  },
  "dependencies": {
    "nuxt": "^4.3.1",
    "vue": "^3.5.30",
    "vue-router": "^5.0.3"
  },
  "devDependencies": {
    "@connectrpc/connect-web": "^2.1.1",
    "@nuxt/ui": "^4.5.1"
  }
}
